{
  "name": "studio-archive-ai",
  "version": "1.0.0",
  "description": "AI-powered chat interface for student projects archive",
  "main": "server/server.js",
  "scripts": {
    "server": "cd server && node server.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["ai", "chat", "deepseek", "student-projects"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
